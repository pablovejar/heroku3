
<%= simple_form_for([@restaurant, @dish, @review]) do |form| %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>


  <div class="actions">
    <%= form.button :submit %>
  </div>
<% end %>
