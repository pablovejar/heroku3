<!DOCTYPE html>
<html>
   <head>
      <title>UserEats</title>
   </head>
   <div class="title-container">
      <h1>Bienvenido a la página UserEats</h1>
      <% if user_signed_in? %>
         <%= link_to "Nuevo Restaurante", new_restaurant_path, class: "btn btn-default"%>
      <% end %>
   </div>

   <div class="restaurants">
      <% Restaurant.all.each do |restaurant| %>
         <div class="restaurant">
            <div class="thumbnail">
            <div class="caption">
               <h3><%= link_to truncate(restaurant.name), restaurant_path(restaurant)%></h3>
               <p><%= truncate(restaurant.address, lenght: 150)%></p>
               <p>
               <% if user_signed_in? %>
                 <% if current_user.id == restaurant.user_id%>
                  <%= link_to "Edit", edit_restaurant_path(restaurant), class: "btn btn-default"%>
                  <%= link_to "Delete", restaurant_path(restaurant), data: {confirm: "Seguro que quieres eliminar el restaurant: #{restaurant} ?", 
                  }, method: :delete %>
                  <%end%>
               <%end%>
               </p>
            </div>
            </div>
         </div>
      <% end %>
   </div>

</html>